<Page
    x:Class="WindowsAiAssistant.App.Views.CapabilitiesPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:muxc="using:Microsoft.UI.Xaml.Controls">

    <Grid Padding="{StaticResource PagePadding}" RowSpacing="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Spacing="2">
            <TextBlock Text="Yetenekler" Style="{StaticResource PageTitleTextBlockStyle}" />
            <TextBlock Text="{Binding CountSummary}" Style="{StaticResource PageSubtitleTextBlockStyle}" />
        </StackPanel>

        <muxc:AutoSuggestBox Grid.Row="1"
                             PlaceholderText="Yetenek arayın (ör. Application, Service)"
                             QueryIcon="Find"
                             Text="{Binding SearchText, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />

        <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto">
            <muxc:ItemsRepeater ItemsSource="{Binding Items}">
                <muxc:ItemsRepeater.Layout>
                    <muxc:UniformGridLayout MinItemWidth="260"
                                            MinItemHeight="120"
                                            MinRowSpacing="12"
                                            MinColumnSpacing="12" />
                </muxc:ItemsRepeater.Layout>
                <muxc:ItemsRepeater.ItemTemplate>
                    <DataTemplate>
                        <Border Style="{StaticResource CardBorderStyle}">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="*" />
                                </Grid.ColumnDefinitions>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                </Grid.RowDefinitions>

                                <Border Grid.RowSpan="3"
                                        Width="44"
                                        Height="44"
                                        VerticalAlignment="Top"
                                        Margin="0,0,12,0"
                                        CornerRadius="8"
                                        Background="{ThemeResource AccentFillColorTertiaryBrush}">
                                    <FontIcon Glyph="{Binding IconGlyph}"
                                              FontFamily="{StaticResource SymbolThemeFontFamily}"
                                              FontSize="20" />
                                </Border>

                                <TextBlock Grid.Column="1" Grid.Row="0"
                                           Text="{Binding Name}"
                                           FontWeight="SemiBold"
                                           FontSize="15" />
                                <TextBlock Grid.Column="1" Grid.Row="1"
                                           Text="{Binding Description}"
                                           Opacity="0.75"
                                           TextWrapping="Wrap"
                                           Margin="0,4,0,8" />
                                <StackPanel Grid.Column="1" Grid.Row="2"
                                            Orientation="Horizontal"
                                            Spacing="6">
                                    <TextBlock Style="{StaticResource LabelTextBlockStyle}" Text="HEDEFLER" />
                                    <TextBlock Text="{Binding TargetKindsDisplay}" FontSize="12" />
                                </StackPanel>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </muxc:ItemsRepeater.ItemTemplate>
            </muxc:ItemsRepeater>
        </ScrollViewer>
    </Grid>
</Page>
