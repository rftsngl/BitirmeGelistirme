{
  "Agent": {
    "MaxSteps": 10,
    "MaxPriorStepsInPrompt": 10,
    "SystemPrompt": "You are an autonomous Windows desktop operator. The user states a GOAL; YOU decide whether it needs desktop actions or a direct Turkish reply. Loop: observe → pick one tool (shell, UI automation, launch) OR respond → execute → verify → respond with feedback. For greetings, small talk and general questions use respond/complete in one step with no desktop automation. For desktop goals, carry the work through yourself using observation feedback; a failed action is feedback — try another route before giving up. Never automate the Windows AI Assistant chat window. Output only the requested strict JSON decision object. User-facing messages must be in Turkish. Safety: avoid or require approval for irreversible, destructive, privilege-escalating or secret-exposing operations; never expose credentials or private data.",
    "Model": {
      "Provider": "OpenAICompatible",
      "BaseUrl": "https://api.openai.com/v1",
      "Endpoint": "/chat/completions",
      "Model": "gpt-4o-mini",
      "ApiKeyEnvironmentVariable": "OPENAI_API_KEY",
      "RequestTimeoutSeconds": 120
    }
  },
  "Runtime": {
    "LogsDirectory": "logs/runs",
    "ScreenshotsDirectory": "logs/screenshots",
    "UiAutomation": {
      "MaxDepth": 4,
      "MaxElementsPerStep": 80,
      "CaptureTimeoutMs": 8000,
      "EnableChromiumAccessibility": true,
      "EnableUia2Fallback": true,
      "Uia2FallbackMinElements": 3
    },
    "ActionPolicy": {
      "Normal": "Allow",
      "Sensitive": "RequireApproval",
      "Destructive": "RequireApproval",
      "AllowSessionRemember": false
    }
  },
  "Audio": {
    "BackgroundModeEnabled": true,
    "StartMinimizedToTray": true,
    "WakeWordEnabled": false,
    "WakeWordPhrase": "asistan",
    "GlobalHotKeyEnabled": true,
    "GlobalHotKey": "Ctrl+Alt+A",
    "TextToSpeechEnabled": true,
    "SpeechLanguage": "tr-TR",
    "SpeechListenTimeoutSeconds": 12,
    "OverlayAutoCloseSeconds": 10,
    "StartWithWindows": false,
    "VoiceApprovalEnabled": true,
    "SpeechEngine": "vosk",
    "WhisperModelPath": "",
    "VoskModelPath": "",
    "InputDeviceIndex": -1
  }
}
