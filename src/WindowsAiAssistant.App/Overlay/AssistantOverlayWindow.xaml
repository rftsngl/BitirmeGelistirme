<Window
    x:Class="WindowsAiAssistant.App.Overlay.AssistantOverlayWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:muxc="using:Microsoft.UI.Xaml.Controls">

    <Border Background="{ThemeResource LayerFillColorDefaultBrush}"
            CornerRadius="16"
            Padding="20,16">
        <Grid x:Name="Root" KeyDown="Root_KeyDown">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <StackPanel Orientation="Horizontal" Spacing="8">
                <FontIcon Glyph="&#xE1D6;"
                          FontFamily="{StaticResource SymbolThemeFontFamily}"
                          Foreground="{ThemeResource AccentTextFillColorPrimaryBrush}" />
                <TextBlock Text="{Binding StatusText}"
                           FontSize="18"
                           FontWeight="SemiBold"
                           VerticalAlignment="Center" />
            </StackPanel>
            <Button Grid.Column="1"
                    Click="CloseButton_Click"
                    Background="Transparent"
                    BorderThickness="0"
                    ToolTipService.ToolTip="Kapat (ESC)">
                <FontIcon Glyph="&#xE711;" FontFamily="{StaticResource SymbolThemeFontFamily}" />
            </Button>
        </Grid>

        <TextBlock Grid.Row="1"
                   Text="{Binding DetailText}"
                   Opacity="0.85"
                   TextWrapping="Wrap"
                   Margin="0,0,0,8" />

        <StackPanel Grid.Row="2" Spacing="8">
            <TextBlock Text="{Binding TranscriptText}"
                       FontStyle="Italic"
                       Opacity="0.9"
                       TextWrapping="Wrap"
                       Visibility="{Binding TranscriptText, Converter={StaticResource StringNotEmptyToVisibilityConverter}}" />
            <TextBlock Text="{Binding ResultText}"
                       TextWrapping="Wrap"
                       Visibility="{Binding ResultText, Converter={StaticResource StringNotEmptyToVisibilityConverter}}" />
            <muxc:ProgressRing IsActive="{Binding IsBusy}"
                               Width="28"
                               Height="28"
                               HorizontalAlignment="Left"
                               Visibility="{Binding IsBusy, Converter={StaticResource BoolToVisibilityConverter}}" />
        </StackPanel>

        <TextBlock Grid.Row="3"
                   Text="ESC ile kapat · Ctrl+Alt+A ile tekrar ac"
                   FontSize="11"
                   Opacity="0.55"
                   HorizontalAlignment="Right"
                   Margin="0,8,0,0" />

        <StackPanel Grid.Row="3"
                    Orientation="Horizontal"
                    Spacing="8"
                    HorizontalAlignment="Right"
                    Margin="0,8,0,0"
                    Visibility="{Binding IsAwaitingApproval, Converter={StaticResource BoolToVisibilityConverter}}">
            <Button x:Name="ApproveButton"
                    Content="Onayla"
                    Style="{ThemeResource AccentButtonStyle}"
                    Click="ApproveButton_Click" />
            <Button x:Name="DenyButton"
                    Content="Reddet"
                    Click="DenyButton_Click" />
        </StackPanel>
        </Grid>
    </Border>
</Window>
